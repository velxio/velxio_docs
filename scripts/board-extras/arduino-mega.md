## About this board

The ATmega2560 at 16 MHz with 256 KB of flash — the pin-hungry member of the
family, with 54 digital I/O against the UNO's 14. That pin count, not the
clock speed, is what usually decides a project onto a Mega.

The pin map below is the board's own, all 85 entries, and it is one of only
three in the catalog that carries **signal names**: the PWM-capable pins, the
USART pins, the I2C pair on `20`/`21` and the SPI set on `50`/`51`/`52` are
labelled from the board's metadata rather than a table maintained here.

## Start here

[New Arduino Mega project](https://velxio.dev/example/mega-blink) opens the
editor with this board and a working blink sketch.
